<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- LayerBackdropView captures its content to a bitmap layer -->
    <com.kyant.backdrop.xml.backdrop.LayerBackdropView
        android:id="@+id/backdropLayer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Background image that will be captured -->
        <ImageView
            android:id="@+id/backgroundImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/wallpaper" />

    </com.kyant.backdrop.xml.backdrop.LayerBackdropView>

    <!-- Glass view that uses the layer as backdrop -->
    <com.kyant.backdrop.xml.views.LiquidGlassContainer
        android:id="@+id/glassContainer"
        android:layout_width="300dp"
        android:layout_height="200dp"
        android:layout_gravity="center"
        app:cornerRadius="24dp"
        app:refractionHeight="80dp"
        app:refractionAmount="40dp"
        app:dispersionHeight="60dp"
        app:dispersionAmount="30dp"
        app:highlightAngle="6.2"
        app:highlightAlpha="0.6"
        app:innerShadowRadius="20dp"
        app:innerShadowAlpha="0.3">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Liquid Glass Effect"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:textStyle="bold" />

    </com.kyant.backdrop.xml.views.LiquidGlassContainer>

</FrameLayout>
