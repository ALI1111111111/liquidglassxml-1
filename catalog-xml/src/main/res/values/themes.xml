<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Standalone theme for the XML catalog -->
    <style name="Theme.LiquidGlassXmlCatalog" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="colorPrimary">@color/glass_primary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
    
    <!-- Glass-specific styles for demo purposes -->
    <style name="GlassContainer">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:elevation">8dp</item>
    </style>
    
    <style name="DemoButton" parent="@android:style/Widget.Material.Button">
        <item name="android:layout_margin">8dp</item>
        <item name="android:padding">16dp</item>
        <item name="android:textSize">16sp</item>
    </style>
    
    <style name="DemoCard">
        <item name="android:layout_margin">16dp</item>
        <item name="android:padding">24dp</item>
        <item name="android:background">@android:color/white</item>
        <item name="android:elevation">4dp</item>
    </style>
</resources>
